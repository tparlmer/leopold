version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - gosimple
    - typecheck

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
